// SPDX-License-Identifier: Apache-2.0
// @generated by scripts/generate-bindings.py from docs/derived-core-schema.json -- DO NOT EDIT.
#include "generated.h"
#include "generated_resolvers.h"

using namespace odb;

rust::String block_get_name(const OdbDb& h) { auto* p = gen_block(h); return p ? rust::String(p->getName()) : rust::String(); }
rust::String block_get_const_name(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
rust::String block_get_chip(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); auto* t = p->getChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String block_get_tech(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); auto* t = p->getTech(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String block_get_parent(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String block_get_parent_inst(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); auto* t = p->getParentInst(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String block_get_top_module(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); auto* t = p->getTopModule(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_block_get_children(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getChildren().size() : 0; }
rust::String nth_block_get_children(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChildren()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_b_terms(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getBTerms().size() : 0; }
rust::String nth_block_get_b_terms(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getBTerms()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_i_terms(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getITerms().size() : 0; }
rust::String nth_block_get_i_terms(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getITerms()) { if (k++ == i) return rust::String((e->getInst()->getName() + "/" + e->getMTerm()->getName())); } return rust::String(); }
std::size_t num_block_get_insts(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getInsts().size() : 0; }
rust::String nth_block_get_insts(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getInsts()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_modules(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getModules().size() : 0; }
rust::String nth_block_get_modules(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModules()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_mod_insts(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getModInsts().size() : 0; }
rust::String nth_block_get_mod_insts(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModInsts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_mod_nets(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getModNets().size() : 0; }
rust::String nth_block_get_mod_nets(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModNets()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_mod_b_terms(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getModBTerms().size() : 0; }
rust::String nth_block_get_mod_b_terms(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModBTerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_mod_i_terms(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getModITerms().size() : 0; }
rust::String nth_block_get_mod_i_terms(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModITerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_power_domains(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getPowerDomains().size() : 0; }
rust::String nth_block_get_power_domains(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getPowerDomains()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_logic_ports(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getLogicPorts().size() : 0; }
rust::String nth_block_get_logic_ports(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getLogicPorts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_power_switches(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getPowerSwitches().size() : 0; }
rust::String nth_block_get_power_switches(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getPowerSwitches()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_isolations(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getIsolations().size() : 0; }
rust::String nth_block_get_isolations(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getIsolations()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_level_shifters(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getLevelShifters().size() : 0; }
rust::String nth_block_get_level_shifters(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getLevelShifters()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_groups(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getGroups().size() : 0; }
rust::String nth_block_get_groups(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getGroups()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_component_mask_shift(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getComponentMaskShift().size() : 0; }
rust::String nth_block_get_component_mask_shift(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); auto v = p->getComponentMaskShift(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getConstName()); }
std::size_t num_block_get_nets(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getNets().size() : 0; }
rust::String nth_block_get_nets(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getNets()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_vias(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getVias().size() : 0; }
rust::String nth_block_get_vias(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getVias()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
int32_t block_get_def_units(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDefUnits() : 0; }
int32_t block_get_db_units_per_micron(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDbUnitsPerMicron() : 0; }
int32_t block_get_corner_count(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCornerCount() : 0; }
int32_t block_get_corners_per_block(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCornersPerBlock() : 0; }
int32_t block_get_ext_db_count(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getExtDbCount() : 0; }
rust::String block_get_corner_name_list(const OdbDb& h) { auto* p = gen_block(h); if (!p) return rust::String(); const char* v = p->getCornerNameList(); return rust::String(v ? v : ""); }
std::size_t num_block_get_rows(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getRows().size() : 0; }
rust::String nth_block_get_rows(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getRows()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
int32_t block_get_die_area_x_min(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().xMin() : 0; }
int32_t block_get_die_area_y_min(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().yMin() : 0; }
int32_t block_get_die_area_x_max(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().xMax() : 0; }
int32_t block_get_die_area_y_max(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().yMax() : 0; }
int32_t block_get_die_area_dx(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().dx() : 0; }
int32_t block_get_die_area_dy(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getDieArea().dy() : 0; }
int32_t block_get_core_area_x_min(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().xMin() : 0; }
int32_t block_get_core_area_y_min(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().yMin() : 0; }
int32_t block_get_core_area_x_max(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().xMax() : 0; }
int32_t block_get_core_area_y_max(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().yMax() : 0; }
int32_t block_get_core_area_dx(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().dx() : 0; }
int32_t block_get_core_area_dy(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getCoreArea().dy() : 0; }
int32_t block_get_min_routing_layer(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getMinRoutingLayer() : 0; }
int32_t block_get_max_routing_layer(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getMaxRoutingLayer() : 0; }
int32_t block_get_min_layer_for_clock(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getMinLayerForClock() : 0; }
int32_t block_get_max_layer_for_clock(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getMaxLayerForClock() : 0; }
int32_t block_get_g_cell_tile_size(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getGCellTileSize() : 0; }
std::size_t num_block_get_regions(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getRegions().size() : 0; }
rust::String nth_block_get_regions(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getRegions()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_block_get_non_default_rules(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getNonDefaultRules().size() : 0; }
rust::String nth_block_get_non_default_rules(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getNonDefaultRules()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_block_get_marker_categories(const OdbDb& h) { auto* p = gen_block(h); return p ? p->getMarkerCategories().size() : 0; }
rust::String nth_block_get_marker_categories(const OdbDb& h, std::size_t i) { auto* p = gen_block(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMarkerCategories()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String inst_get_name(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? rust::String(p->getName()) : rust::String(); }
rust::String inst_get_const_name(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
int32_t inst_get_origin_x(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getOrigin().getX() : 0; }
int32_t inst_get_origin_y(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getOrigin().getY() : 0; }
rust::String inst_get_orient(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? rust::String(p->getOrient().getString()) : rust::String(); }
int32_t inst_get_location_x(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getLocation().getX() : 0; }
int32_t inst_get_location_y(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getLocation().getY() : 0; }
rust::String inst_get_placement_status(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? rust::String(p->getPlacementStatus().getString()) : rust::String(); }
bool inst_is_fixed(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isFixed() : false; }
bool inst_is_placed(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isPlaced() : false; }
bool inst_get_eco_create(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getEcoCreate() : false; }
bool inst_get_eco_destroy(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getEcoDestroy() : false; }
bool inst_get_eco_modify(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getEcoModify() : false; }
bool inst_get_user_flag1(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getUserFlag1() : false; }
bool inst_get_user_flag2(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getUserFlag2() : false; }
bool inst_get_user_flag3(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getUserFlag3() : false; }
bool inst_is_do_not_touch(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isDoNotTouch() : false; }
rust::String inst_get_block(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String inst_get_master(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getMaster(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String inst_get_group(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getGroup(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_inst_get_i_terms(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getITerms().size() : 0; }
rust::String nth_inst_get_i_terms(const OdbDb& h, rust::Str inst, std::size_t i) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getITerms()) { if (k++ == i) return rust::String((e->getInst()->getName() + "/" + e->getMTerm()->getName())); } return rust::String(); }
rust::String inst_get_first_input(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getFirstInput(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
rust::String inst_get_first_output(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getFirstOutput(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
rust::String inst_get_region(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getRegion(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String inst_get_module(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getModule(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String inst_get_child(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getChild(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String inst_get_parent(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_inst_get_children(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getChildren().size() : 0; }
rust::String nth_inst_get_children(const OdbDb& h, rust::Str inst, std::size_t i) { auto* p = gen_inst(h, inst); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChildren()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
bool inst_is_hierarchical(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isHierarchical() : false; }
bool inst_is_physical_only(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isPhysicalOnly() : false; }
int32_t inst_get_transformed_halo_x_min(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().xMin() : 0; }
int32_t inst_get_transformed_halo_y_min(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().yMin() : 0; }
int32_t inst_get_transformed_halo_x_max(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().xMax() : 0; }
int32_t inst_get_transformed_halo_y_max(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().yMax() : 0; }
int32_t inst_get_transformed_halo_dx(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().dx() : 0; }
int32_t inst_get_transformed_halo_dy(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getTransformedHalo().dy() : 0; }
int32_t inst_get_weight(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getWeight() : 0; }
rust::String inst_get_source_type(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? rust::String(p->getSourceType().getString()) : rust::String(); }
bool inst_is_block(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isBlock() : false; }
bool inst_is_core(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isCore() : false; }
bool inst_is_pad(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isPad() : false; }
bool inst_is_end_cap(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->isEndCap() : false; }
uint32_t inst_get_pin_access_idx(const OdbDb& h, rust::Str inst) { auto* p = gen_inst(h, inst); return p ? p->getPinAccessIdx() : 0; }
rust::String net_get_name(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? rust::String(p->getName()) : rust::String(); }
rust::String net_get_const_name(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
bool net_is_r_c_disconnected(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isRCDisconnected() : false; }
int32_t net_get_weight(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getWeight() : 0; }
rust::String net_get_source_type(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? rust::String(p->getSourceType().getString()) : rust::String(); }
int32_t net_get_x_talk_class(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getXTalkClass() : 0; }
rust::String net_get_driving_i_term(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->getDrivingITerm(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
bool net_has_fixed_bump(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->hasFixedBump() : false; }
rust::String net_get_wire_type(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? rust::String(p->getWireType().getString()) : rust::String(); }
rust::String net_get_sig_type(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? rust::String(p->getSigType().getString()) : rust::String(); }
bool net_is_marked(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isMarked() : false; }
bool net_is_mark_1ed(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isMark_1ed() : false; }
bool net_is_select(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isSelect() : false; }
bool net_is_wire_ordered(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isWireOrdered() : false; }
bool net_is_disconnected(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isDisconnected() : false; }
bool net_is_wire_altered(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isWireAltered() : false; }
bool net_is_r_cgraph(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isRCgraph() : false; }
bool net_is_extracted(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isExtracted() : false; }
bool net_is_spef(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isSpef() : false; }
bool net_is_do_not_touch(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isDoNotTouch() : false; }
rust::String net_get_block(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_net_get_i_terms(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getITerms().size() : 0; }
rust::String nth_net_get_i_terms(const OdbDb& h, rust::Str net, std::size_t i) { auto* p = gen_net(h, net); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getITerms()) { if (k++ == i) return rust::String((e->getInst()->getName() + "/" + e->getMTerm()->getName())); } return rust::String(); }
rust::String net_get1st_i_term(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->get1stITerm(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
rust::String net_get_first_driver_inst(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->getFirstDriverInst(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String net_get_first_output(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->getFirstOutput(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
std::size_t num_net_get_b_terms(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getBTerms().size() : 0; }
rust::String nth_net_get_b_terms(const OdbDb& h, rust::Str net, std::size_t i) { auto* p = gen_net(h, net); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getBTerms()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
rust::String net_get1st_b_term(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->get1stBTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool net_is_connected_by_abutment(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isConnectedByAbutment() : false; }
bool net_is_i_o(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isIO() : false; }
bool net_is_wild_connected(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->isWildConnected() : false; }
float net_get_gndc_calib_factor(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getGndcCalibFactor() : 0.0f; }
float net_get_cc_calib_factor(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCcCalibFactor() : 0.0f; }
float net_get_cc_adjust_factor(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCcAdjustFactor() : 0.0f; }
uint32_t net_get_cc_adjust_order(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCcAdjustOrder() : 0; }
float net_get_db_cc(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getDbCc() : 0.0f; }
float net_get_ref_cc(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getRefCc() : 0.0f; }
float net_get_cc_match_ratio(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCcMatchRatio() : 0.0f; }
uint32_t net_get1st_r_seg_id(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->get1stRSegId() : 0; }
uint32_t net_get1st_cap_node_id(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->get1stCapNodeId() : 0; }
uint32_t net_get_r_seg_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getRSegCount() : 0; }
uint32_t net_max_internal_cap_num(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->maxInternalCapNum() : 0; }
uint32_t net_get_cap_node_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCapNodeCount() : 0; }
uint32_t net_get_cc_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getCcCount() : 0; }
rust::String net_get_non_default_rule(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->getNonDefaultRule(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t net_get_term_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermCount() : 0; }
uint32_t net_get_i_term_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getITermCount() : 0; }
uint32_t net_get_b_term_count(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getBTermCount() : 0; }
int32_t net_get_term_b_box_x_min(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().xMin() : 0; }
int32_t net_get_term_b_box_y_min(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().yMin() : 0; }
int32_t net_get_term_b_box_x_max(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().xMax() : 0; }
int32_t net_get_term_b_box_y_max(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().yMax() : 0; }
int32_t net_get_term_b_box_dx(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().dx() : 0; }
int32_t net_get_term_b_box_dy(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->getTermBBox().dy() : 0; }
rust::String net_find_main_parent_mod_inst(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->findMainParentModInst(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String net_find_main_parent_module(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->findMainParentModule(); return t ? rust::String(t->getName()) : rust::String(); }
bool net_has_jumpers(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); return p ? p->hasJumpers() : false; }
rust::String net_find_mod_net_in_highest_hier(const OdbDb& h, rust::Str net) { auto* p = gen_net(h, net); if (!p) return rust::String(); auto* t = p->findModNetInHighestHier(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t net_get_wire_count_wire_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getWireCount(v0, v1); return v0; }
uint32_t net_get_wire_count_via_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getWireCount(v0, v1); return v1; }
uint32_t net_get_signal_wire_count_wire_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getSignalWireCount(v0, v1); return v0; }
uint32_t net_get_signal_wire_count_via_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getSignalWireCount(v0, v1); return v1; }
uint32_t net_get_power_wire_count_wire_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getPowerWireCount(v0, v1); return v0; }
uint32_t net_get_power_wire_count_via_cnt(const OdbDb& h, rust::Str net) { uint32_t v0 = 0; uint32_t v1 = 0; auto* p = gen_net(h, net); if (p) p->getPowerWireCount(v0, v1); return v1; }
rust::String bterm_get_name(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? rust::String(p->getName()) : rust::String(); }
rust::String bterm_get_const_name(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
int32_t bterm_get_b_box_x_min(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().xMin() : 0; }
int32_t bterm_get_b_box_y_min(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().yMin() : 0; }
int32_t bterm_get_b_box_x_max(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().xMax() : 0; }
int32_t bterm_get_b_box_y_max(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().yMax() : 0; }
int32_t bterm_get_b_box_dx(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().dx() : 0; }
int32_t bterm_get_b_box_dy(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getBBox().dy() : 0; }
rust::String bterm_get_sig_type(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? rust::String(p->getSigType().getString()) : rust::String(); }
rust::String bterm_get_io_type(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? rust::String(p->getIoType().getString()) : rust::String(); }
bool bterm_is_set_spef_mark(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->isSetSpefMark() : false; }
bool bterm_is_set_mark(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->isSetMark() : false; }
uint32_t bterm_get_ext_id(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->getExtId() : 0; }
bool bterm_is_special(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->isSpecial() : false; }
rust::String bterm_get_net(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String bterm_get_mod_net(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getModNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String bterm_get_block(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String bterm_get_i_term(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getITerm(); return t ? rust::String((t->getInst()->getName() + "/" + t->getMTerm()->getName())) : rust::String(); }
rust::String bterm_get_first_pin_placement_status(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? rust::String(p->getFirstPinPlacementStatus().getString()) : rust::String(); }
rust::String bterm_get_ground_pin(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getGroundPin(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String bterm_get_supply_pin(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getSupplyPin(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t bterm_sta_vertex_id(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->staVertexId() : 0; }
rust::String bterm_get_mirrored_b_term(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); if (!p) return rust::String(); auto* t = p->getMirroredBTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool bterm_has_mirrored_b_term(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->hasMirroredBTerm() : false; }
bool bterm_is_mirrored(const OdbDb& h, rust::Str bterm) { auto* p = gen_bterm(h, bterm); return p ? p->isMirrored() : false; }
rust::String master_get_name(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? rust::String(p->getName()) : rust::String(); }
rust::String master_get_const_name(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
int32_t master_get_origin_x(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getOrigin().getX() : 0; }
int32_t master_get_origin_y(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getOrigin().getY() : 0; }
uint32_t master_get_width(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getWidth() : 0; }
uint32_t master_get_height(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getHeight() : 0; }
bool master_is_filler(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isFiller() : false; }
bool master_is_block(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isBlock() : false; }
bool master_is_core(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isCore() : false; }
bool master_is_pad(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isPad() : false; }
bool master_is_end_cap(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isEndCap() : false; }
bool master_is_cover(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isCover() : false; }
bool master_is_core_auto_placeable(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isCoreAutoPlaceable() : false; }
bool master_is_backside_bridge(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isBacksideBridge() : false; }
rust::String master_get_l_e_q(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); if (!p) return rust::String(); auto* t = p->getLEQ(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String master_get_e_e_q(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); if (!p) return rust::String(); auto* t = p->getEEQ(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool master_get_symmetry_x(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getSymmetryX() : false; }
bool master_get_symmetry_y(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getSymmetryY() : false; }
bool master_get_symmetry_r90(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getSymmetryR90() : false; }
std::size_t num_master_get_m_terms(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getMTerms().size() : 0; }
rust::String nth_master_get_m_terms(const OdbDb& h, rust::Str master, std::size_t i) { auto* p = gen_master(h, master); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMTerms()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
rust::String master_get_lib(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); if (!p) return rust::String(); auto* t = p->getLib(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool master_is_frozen(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isFrozen() : false; }
bool master_is_sequential(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isSequential() : false; }
uint32_t master_is_marked(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isMarked() : 0; }
bool master_is_special_power(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->isSpecialPower() : false; }
int32_t master_get_m_term_count(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getMTermCount() : 0; }
rust::String master_get_site(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); if (!p) return rust::String(); auto* t = p->getSite(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t master_get_master_id(const OdbDb& h, rust::Str master) { auto* p = gen_master(h, master); return p ? p->getMasterId() : 0; }
rust::String iterm_get_inst(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getInst(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String iterm_get_net(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String iterm_get_mod_net(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getModNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String iterm_get_m_term(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getMTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t iterm_get_b_box_x_min(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().xMin() : 0; }
int32_t iterm_get_b_box_y_min(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().yMin() : 0; }
int32_t iterm_get_b_box_x_max(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().xMax() : 0; }
int32_t iterm_get_b_box_y_max(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().yMax() : 0; }
int32_t iterm_get_b_box_dx(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().dx() : 0; }
int32_t iterm_get_b_box_dy(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getBBox().dy() : 0; }
rust::String iterm_get_block(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String iterm_get_sig_type(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? rust::String(p->getSigType().getString()) : rust::String(); }
rust::String iterm_get_io_type(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? rust::String(p->getIoType().getString()) : rust::String(); }
bool iterm_is_spef(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->isSpef() : false; }
uint32_t iterm_get_ext_id(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->getExtId() : 0; }
bool iterm_is_special(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->isSpecial() : false; }
bool iterm_is_clocked(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->isClocked() : false; }
bool iterm_is_set_mark(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->isSetMark() : false; }
bool iterm_is_connected(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->isConnected() : false; }
rust::String iterm_get_b_term(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); if (!p) return rust::String(); auto* t = p->getBTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t iterm_sta_vertex_id(const OdbDb& h, rust::Str inst, rust::Str pin) { auto* p = gen_iterm(h, inst, pin); return p ? p->staVertexId() : 0; }
rust::String mterm_get_name(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? rust::String(p->getName()) : rust::String(); }
rust::String mterm_get_const_name(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
rust::String mterm_get_sig_type(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? rust::String(p->getSigType().getString()) : rust::String(); }
rust::String mterm_get_io_type(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? rust::String(p->getIoType().getString()) : rust::String(); }
bool mterm_is_set_mark(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->isSetMark() : false; }
rust::String mterm_get_master(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); if (!p) return rust::String(); auto* t = p->getMaster(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t mterm_get_b_box_x_min(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().xMin() : 0; }
int32_t mterm_get_b_box_y_min(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().yMin() : 0; }
int32_t mterm_get_b_box_x_max(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().xMax() : 0; }
int32_t mterm_get_b_box_y_max(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().yMax() : 0; }
int32_t mterm_get_b_box_dx(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().dx() : 0; }
int32_t mterm_get_b_box_dy(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getBBox().dy() : 0; }
bool mterm_has_default_antenna_model(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->hasDefaultAntennaModel() : false; }
bool mterm_has_oxide2_antenna_model(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->hasOxide2AntennaModel() : false; }
int32_t mterm_get_index(const OdbDb& h, rust::Str master, rust::Str term) { auto* p = gen_mterm(h, master, term); return p ? p->getIndex() : 0; }
uint32_t layer_get_wrong_way_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getWrongWayWidth() : 0; }
uint32_t layer_get_wrong_way_min_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getWrongWayMinWidth() : 0; }
float layer_get_layer_adjustment(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getLayerAdjustment() : 0.0f; }
std::size_t num_layer_get_tech_layer_cut_class_rules(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getTechLayerCutClassRules().size() : 0; }
rust::String nth_layer_get_tech_layer_cut_class_rules(const OdbDb& h, rust::Str layer, std::size_t i) { auto* p = gen_techlayer(h, layer); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getTechLayerCutClassRules()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
bool layer_is_rect_only(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->isRectOnly() : false; }
bool layer_is_right_way_on_grid_only(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->isRightWayOnGridOnly() : false; }
bool layer_is_right_way_on_grid_only_check_mask(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->isRightWayOnGridOnlyCheckMask() : false; }
bool layer_is_rect_only_except_non_core_pins(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->isRectOnlyExceptNonCorePins() : false; }
rust::String layer_get_lef58_type_string(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? rust::String(p->getLef58TypeString()) : rust::String(); }
bool layer_is_backside(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->isBackside() : false; }
rust::String layer_get_name(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? rust::String(p->getName()) : rust::String(); }
rust::String layer_get_const_name(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
bool layer_has_alias(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasAlias() : false; }
rust::String layer_get_alias(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? rust::String(p->getAlias()) : rust::String(); }
uint32_t layer_get_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getWidth() : 0; }
int32_t layer_get_spacing(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getSpacing() : 0; }
uint32_t layer_get_num_masks(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getNumMasks() : 0; }
bool layer_has_v55_spacing_rules(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasV55SpacingRules() : false; }
bool layer_has_two_widths_spacing_rules(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasTwoWidthsSpacingRules() : false; }
uint32_t layer_get_two_widths_spacing_table_num_widths(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getTwoWidthsSpacingTableNumWidths() : 0; }
bool layer_has_default_antenna_rule(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasDefaultAntennaRule() : false; }
bool layer_has_oxide2_antenna_rule(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasOxide2AntennaRule() : false; }
int32_t layer_get_pitch(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getPitch() : 0; }
int32_t layer_get_pitch_x(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getPitchX() : 0; }
int32_t layer_get_pitch_y(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getPitchY() : 0; }
int32_t layer_get_first_last_pitch(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getFirstLastPitch() : 0; }
bool layer_has_x_y_pitch(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasXYPitch() : false; }
int32_t layer_get_offset(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getOffset() : 0; }
int32_t layer_get_offset_x(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getOffsetX() : 0; }
int32_t layer_get_offset_y(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getOffsetY() : 0; }
bool layer_has_x_y_offset(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasXYOffset() : false; }
bool layer_has_area(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasArea() : false; }
bool layer_has_max_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasMaxWidth() : false; }
uint32_t layer_get_max_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getMaxWidth() : 0; }
uint32_t layer_get_min_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getMinWidth() : 0; }
bool layer_has_min_step(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasMinStep() : false; }
uint32_t layer_get_min_step(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getMinStep() : 0; }
bool layer_has_min_step_max_length(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasMinStepMaxLength() : false; }
uint32_t layer_get_min_step_max_length(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getMinStepMaxLength() : 0; }
bool layer_has_min_step_max_edges(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasMinStepMaxEdges() : false; }
uint32_t layer_get_min_step_max_edges(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getMinStepMaxEdges() : 0; }
bool layer_has_protrusion(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasProtrusion() : false; }
uint32_t layer_get_protrusion_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getProtrusionWidth() : 0; }
uint32_t layer_get_protrusion_length(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getProtrusionLength() : 0; }
uint32_t layer_get_protrusion_from_width(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getProtrusionFromWidth() : 0; }
double layer_get_resistance(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getResistance() : 0.0; }
double layer_get_capacitance(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getCapacitance() : 0.0; }
double layer_get_edge_capacitance(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getEdgeCapacitance() : 0.0; }
uint32_t layer_get_wire_extension(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getWireExtension() : 0; }
int32_t layer_get_number(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getNumber() : 0; }
int32_t layer_get_routing_level(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->getRoutingLevel() : 0; }
rust::String layer_get_lower_layer(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); if (!p) return rust::String(); auto* t = p->getLowerLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String layer_get_upper_layer(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); if (!p) return rust::String(); auto* t = p->getUpperLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String layer_get_tech(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); if (!p) return rust::String(); auto* t = p->getTech(); return t ? rust::String(t->getName()) : rust::String(); }
bool layer_has_orth_spacing_table(const OdbDb& h, rust::Str layer) { auto* p = gen_techlayer(h, layer); return p ? p->hasOrthSpacingTable() : false; }
int32_t layer_get_max_wide_d_r_c_range_owidth(const OdbDb& h, rust::Str layer) { int v0 = 0; int v1 = 0; auto* p = gen_techlayer(h, layer); if (p) p->getMaxWideDRCRange(v0, v1); return v0; }
int32_t layer_get_max_wide_d_r_c_range_olength(const OdbDb& h, rust::Str layer) { int v0 = 0; int v1 = 0; auto* p = gen_techlayer(h, layer); if (p) p->getMaxWideDRCRange(v0, v1); return v1; }
int32_t layer_get_min_wide_d_r_c_range_owidth(const OdbDb& h, rust::Str layer) { int v0 = 0; int v1 = 0; auto* p = gen_techlayer(h, layer); if (p) p->getMinWideDRCRange(v0, v1); return v0; }
int32_t layer_get_min_wide_d_r_c_range_olength(const OdbDb& h, rust::Str layer) { int v0 = 0; int v1 = 0; auto* p = gen_techlayer(h, layer); if (p) p->getMinWideDRCRange(v0, v1); return v1; }
rust::String row_get_name(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? rust::String(p->getName()) : rust::String(); }
rust::String row_get_const_name(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
rust::String row_get_site(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); if (!p) return rust::String(); auto* t = p->getSite(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t row_get_origin_x(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getOrigin().getX() : 0; }
int32_t row_get_origin_y(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getOrigin().getY() : 0; }
rust::String row_get_orient(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? rust::String(p->getOrient().getString()) : rust::String(); }
int32_t row_get_site_count(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getSiteCount() : 0; }
int32_t row_get_spacing(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getSpacing() : 0; }
int32_t row_get_b_box_x_min(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().xMin() : 0; }
int32_t row_get_b_box_y_min(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().yMin() : 0; }
int32_t row_get_b_box_x_max(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().xMax() : 0; }
int32_t row_get_b_box_y_max(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().yMax() : 0; }
int32_t row_get_b_box_dx(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().dx() : 0; }
int32_t row_get_b_box_dy(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); return p ? p->getBBox().dy() : 0; }
rust::String row_get_block(const OdbDb& h, rust::Str row) { auto* p = gen_row(h, row); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_get_name(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? rust::String(p->getName()) : rust::String(); }
rust::String via_get_const_name(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
rust::String via_get_pattern(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? rust::String(p->getPattern()) : rust::String(); }
rust::String via_get_via_generate_rule(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getViaGenerateRule(); return t ? rust::String(t->getName()) : rust::String(); }
bool via_has_params(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? p->hasParams() : false; }
rust::String via_get_block(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_get_top_layer(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getTopLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_get_bottom_layer(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getBottomLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool via_is_via_rotated(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? p->isViaRotated() : false; }
rust::String via_get_orient(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? rust::String(p->getOrient().getString()) : rust::String(); }
rust::String via_get_tech_via(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getTechVia(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_get_block_via(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); if (!p) return rust::String(); auto* t = p->getBlockVia(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool via_is_default(const OdbDb& h, rust::Str via) { auto* p = gen_via(h, via); return p ? p->isDefault() : false; }
rust::String techvia_get_name(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? rust::String(p->getName()) : rust::String(); }
rust::String techvia_get_const_name(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
bool techvia_is_default(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? p->isDefault() : false; }
bool techvia_is_top_of_stack(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? p->isTopOfStack() : false; }
double techvia_get_resistance(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? p->getResistance() : 0.0; }
rust::String techvia_get_pattern(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? rust::String(p->getPattern()) : rust::String(); }
rust::String techvia_get_via_generate_rule(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); auto* t = p->getViaGenerateRule(); return t ? rust::String(t->getName()) : rust::String(); }
bool techvia_has_params(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); return p ? p->hasParams() : false; }
rust::String techvia_get_tech(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); auto* t = p->getTech(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String techvia_get_top_layer(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); auto* t = p->getTopLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String techvia_get_bottom_layer(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); auto* t = p->getBottomLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String techvia_get_non_default_rule(const OdbDb& h, rust::Str via) { auto* p = gen_techvia(h, via); if (!p) return rust::String(); auto* t = p->getNonDefaultRule(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String ndr_get_name(const OdbDb& h, rust::Str rule) { auto* p = gen_ndr(h, rule); return p ? rust::String(p->getName()) : rust::String(); }
rust::String ndr_get_const_name(const OdbDb& h, rust::Str rule) { auto* p = gen_ndr(h, rule); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
bool ndr_is_block_rule(const OdbDb& h, rust::Str rule) { auto* p = gen_ndr(h, rule); return p ? p->isBlockRule() : false; }
bool ndr_get_hard_spacing(const OdbDb& h, rust::Str rule) { auto* p = gen_ndr(h, rule); return p ? p->getHardSpacing() : false; }
rust::String site_get_name(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? rust::String(p->getName()) : rust::String(); }
rust::String site_get_const_name(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
int32_t site_get_width(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->getWidth() : 0; }
int32_t site_get_height(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->getHeight() : 0; }
bool site_get_symmetry_x(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->getSymmetryX() : false; }
bool site_get_symmetry_y(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->getSymmetryY() : false; }
bool site_get_symmetry_r90(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->getSymmetryR90() : false; }
bool site_has_row_pattern(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->hasRowPattern() : false; }
bool site_is_hybrid(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); return p ? p->isHybrid() : false; }
rust::String site_get_lib(const OdbDb& h, rust::Str site) { auto* p = gen_site(h, site); if (!p) return rust::String(); auto* t = p->getLib(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String obs_get_instance(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); if (!p) return rust::String(); auto* t = p->getInstance(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool obs_is_slot_obstruction(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->isSlotObstruction() : false; }
bool obs_is_fill_obstruction(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->isFillObstruction() : false; }
bool obs_is_except_p_g_nets_obstruction(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->isExceptPGNetsObstruction() : false; }
bool obs_is_pushed_down(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->isPushedDown() : false; }
bool obs_has_effective_width(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->hasEffectiveWidth() : false; }
int32_t obs_get_effective_width(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->getEffectiveWidth() : 0; }
bool obs_has_min_spacing(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->hasMinSpacing() : false; }
int32_t obs_get_min_spacing(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->getMinSpacing() : 0; }
rust::String obs_get_block(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool obs_is_system_reserved(const OdbDb& h, std::size_t idx) { auto* p = gen_obstruction(h, idx); return p ? p->isSystemReserved() : false; }
rust::String swire_get_block(const OdbDb& h, rust::Str net, std::size_t idx) { auto* p = gen_swire(h, net, idx); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String swire_get_net(const OdbDb& h, rust::Str net, std::size_t idx) { auto* p = gen_swire(h, net, idx); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String swire_get_wire_type(const OdbDb& h, rust::Str net, std::size_t idx) { auto* p = gen_swire(h, net, idx); return p ? rust::String(p->getWireType().getString()) : rust::String(); }
rust::String swire_get_shield(const OdbDb& h, rust::Str net, std::size_t idx) { auto* p = gen_swire(h, net, idx); if (!p) return rust::String(); auto* t = p->getShield(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String wire_get_block(const OdbDb& h, rust::Str net) { auto* p = gen_wire(h, net); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String wire_get_net(const OdbDb& h, rust::Str net) { auto* p = gen_wire(h, net); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool wire_is_global_wire(const OdbDb& h, rust::Str net) { auto* p = gen_wire(h, net); return p ? p->isGlobalWire() : false; }
uint32_t wire_length(const OdbDb& h, rust::Str net) { auto* p = gen_wire(h, net); return p ? p->length() : 0; }
uint32_t wire_count(const OdbDb& h, rust::Str net) { auto* p = gen_wire(h, net); return p ? p->count() : 0; }
bool fill_needs_o_p_c(const OdbDb& h, std::size_t idx) { auto* p = gen_fill(h, idx); return p ? p->needsOPC() : false; }
uint32_t fill_mask_number(const OdbDb& h, std::size_t idx) { auto* p = gen_fill(h, idx); return p ? p->maskNumber() : 0; }
rust::String fill_get_tech_layer(const OdbDb& h, std::size_t idx) { auto* p = gen_fill(h, idx); if (!p) return rust::String(); auto* t = p->getTechLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t box_x_min(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->xMin() : 0; }
int32_t box_y_min(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->yMin() : 0; }
int32_t box_x_max(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->xMax() : 0; }
int32_t box_y_max(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->yMax() : 0; }
bool box_is_via(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->isVia() : false; }
rust::String box_get_tech_via(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); if (!p) return rust::String(); auto* t = p->getTechVia(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String box_get_block_via(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); if (!p) return rust::String(); auto* t = p->getBlockVia(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t box_get_via_x_y_x(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getViaXY().getX() : 0; }
int32_t box_get_via_x_y_y(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getViaXY().getY() : 0; }
int32_t box_get_box_x_min(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().xMin() : 0; }
int32_t box_get_box_y_min(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().yMin() : 0; }
int32_t box_get_box_x_max(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().xMax() : 0; }
int32_t box_get_box_y_max(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().yMax() : 0; }
int32_t box_get_box_dx(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().dx() : 0; }
int32_t box_get_box_dy(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getBox().dy() : 0; }
uint32_t box_get_d_x(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getDX() : 0; }
int32_t box_get_design_rule_width(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getDesignRuleWidth() : 0; }
uint32_t box_get_d_y(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getDY() : 0; }
bool box_is_soft(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->isSoft() : false; }
rust::String box_get_tech_layer(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); if (!p) return rust::String(); auto* t = p->getTechLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t box_get_layer_mask(const OdbDb& h, std::size_t idx) { auto* p = gen_box(h, idx); return p ? p->getLayerMask() : 0; }
rust::String module_get_name(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String module_get_mod_inst(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); if (!p) return rust::String(); auto* t = p->getModInst(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String module_get_parent_module(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); if (!p) return rust::String(); auto* t = p->getParentModule(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String module_get_hierarchical_name(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? rust::String(p->getHierarchicalName()) : rust::String(); }
rust::String module_get_owner(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); if (!p) return rust::String(); auto* t = p->getOwner(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_module_get_children(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getChildren().size() : 0; }
rust::String nth_module_get_children(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChildren()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_module_get_mod_insts(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getModInsts().size() : 0; }
rust::String nth_module_get_mod_insts(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModInsts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_module_get_mod_nets(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getModNets().size() : 0; }
rust::String nth_module_get_mod_nets(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModNets()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_module_get_ports(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getPorts().size() : 0; }
rust::String nth_module_get_ports(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getPorts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_module_get_mod_b_terms(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getModBTerms().size() : 0; }
rust::String nth_module_get_mod_b_terms(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModBTerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_module_get_insts(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getInsts().size() : 0; }
rust::String nth_module_get_insts(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getInsts()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_module_get_leaf_insts(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getLeafInsts().size() : 0; }
rust::String nth_module_get_leaf_insts(const OdbDb& h, rust::Str module, std::size_t i) { auto* p = gen_module(h, module); if (!p) return rust::String(); auto v = p->getLeafInsts(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getConstName()); }
int32_t module_get_mod_inst_count(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getModInstCount() : 0; }
int32_t module_get_db_inst_count(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->getDbInstCount() : 0; }
bool module_is_top(const OdbDb& h, rust::Str module) { auto* p = gen_module(h, module); return p ? p->isTop() : false; }
rust::String group_get_name(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String group_get_parent_group(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); if (!p) return rust::String(); auto* t = p->getParentGroup(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String group_get_region(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); if (!p) return rust::String(); auto* t = p->getRegion(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_group_get_mod_insts(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); return p ? p->getModInsts().size() : 0; }
rust::String nth_group_get_mod_insts(const OdbDb& h, rust::Str group, std::size_t i) { auto* p = gen_group(h, group); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModInsts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_group_get_insts(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); return p ? p->getInsts().size() : 0; }
rust::String nth_group_get_insts(const OdbDb& h, rust::Str group, std::size_t i) { auto* p = gen_group(h, group); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getInsts()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_group_get_groups(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); return p ? p->getGroups().size() : 0; }
rust::String nth_group_get_groups(const OdbDb& h, rust::Str group, std::size_t i) { auto* p = gen_group(h, group); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getGroups()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_group_get_power_nets(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); return p ? p->getPowerNets().size() : 0; }
rust::String nth_group_get_power_nets(const OdbDb& h, rust::Str group, std::size_t i) { auto* p = gen_group(h, group); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getPowerNets()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_group_get_ground_nets(const OdbDb& h, rust::Str group) { auto* p = gen_group(h, group); return p ? p->getGroundNets().size() : 0; }
rust::String nth_group_get_ground_nets(const OdbDb& h, rust::Str group, std::size_t i) { auto* p = gen_group(h, group); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getGroundNets()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
rust::String region_get_name(const OdbDb& h, rust::Str region) { auto* p = gen_region(h, region); return p ? rust::String(p->getName()) : rust::String(); }
std::size_t num_region_get_region_insts(const OdbDb& h, rust::Str region) { auto* p = gen_region(h, region); return p ? p->getRegionInsts().size() : 0; }
rust::String nth_region_get_region_insts(const OdbDb& h, rust::Str region, std::size_t i) { auto* p = gen_region(h, region); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getRegionInsts()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
bool region_is_invalid(const OdbDb& h, rust::Str region) { auto* p = gen_region(h, region); return p ? p->isInvalid() : false; }
std::size_t num_region_get_groups(const OdbDb& h, rust::Str region) { auto* p = gen_region(h, region); return p ? p->getGroups().size() : 0; }
rust::String nth_region_get_groups(const OdbDb& h, rust::Str region, std::size_t i) { auto* p = gen_region(h, region); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getGroups()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String region_get_block(const OdbDb& h, rust::Str region) { auto* p = gen_region(h, region); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String blockage_get_instance(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); if (!p) return rust::String(); auto* t = p->getInstance(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool blockage_is_pushed_down(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); return p ? p->isPushedDown() : false; }
bool blockage_is_soft(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); return p ? p->isSoft() : false; }
bool blockage_is_system_reserved(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); return p ? p->isSystemReserved() : false; }
float blockage_get_max_density(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); return p ? p->getMaxDensity() : 0.0f; }
rust::String blockage_get_block(const OdbDb& h, std::size_t idx) { auto* p = gen_blockage(h, idx); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String trackgrid_get_tech_layer(const OdbDb& h, std::size_t idx) { auto* p = gen_trackgrid(h, idx); if (!p) return rust::String(); auto* t = p->getTechLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String trackgrid_get_block(const OdbDb& h, std::size_t idx) { auto* p = gen_trackgrid(h, idx); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t trackgrid_get_num_grid_patterns_x(const OdbDb& h, std::size_t idx) { auto* p = gen_trackgrid(h, idx); return p ? p->getNumGridPatternsX() : 0; }
int32_t trackgrid_get_num_grid_patterns_y(const OdbDb& h, std::size_t idx) { auto* p = gen_trackgrid(h, idx); return p ? p->getNumGridPatternsY() : 0; }
rust::String marker_cat_get_name(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String marker_cat_get_description(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? rust::String(p->getDescription()) : rust::String(); }
int32_t marker_cat_get_max_markers(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? p->getMaxMarkers() : 0; }
std::size_t num_marker_cat_get_markers(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? p->getMarkers().size() : 0; }
rust::String nth_marker_cat_get_markers(const OdbDb& h, rust::Str category, std::size_t i) { auto* p = gen_marker_cat(h, category); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMarkers()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_marker_cat_get_marker_categories(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? p->getMarkerCategories().size() : 0; }
rust::String nth_marker_cat_get_marker_categories(const OdbDb& h, rust::Str category, std::size_t i) { auto* p = gen_marker_cat(h, category); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMarkerCategories()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String marker_cat_get_top_category(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); if (!p) return rust::String(); auto* t = p->getTopCategory(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String marker_cat_get_source(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? rust::String(p->getSource()) : rust::String(); }
int32_t marker_cat_get_marker_count(const OdbDb& h, rust::Str category) { auto* p = gen_marker_cat(h, category); return p ? p->getMarkerCount() : 0; }
rust::String marker_get_comment(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? rust::String(p->getComment()) : rust::String(); }
int32_t marker_get_line_number(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getLineNumber() : 0; }
bool marker_is_visited(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->isVisited() : false; }
bool marker_is_visible(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->isVisible() : false; }
bool marker_is_waived(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->isWaived() : false; }
rust::String marker_get_name(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? rust::String(p->getName()) : rust::String(); }
rust::String marker_get_category(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); if (!p) return rust::String(); auto* t = p->getCategory(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String marker_get_tech_layer(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); if (!p) return rust::String(); auto* t = p->getTechLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t marker_get_b_box_x_min(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().xMin() : 0; }
int32_t marker_get_b_box_y_min(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().yMin() : 0; }
int32_t marker_get_b_box_x_max(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().xMax() : 0; }
int32_t marker_get_b_box_y_max(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().yMax() : 0; }
int32_t marker_get_b_box_dx(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().dx() : 0; }
int32_t marker_get_b_box_dy(const OdbDb& h, rust::Str category, std::size_t idx) { auto* p = gen_marker(h, category, idx); return p ? p->getBBox().dy() : 0; }
rust::String modinst_get_name(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String modinst_get_parent(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String modinst_get_master(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); if (!p) return rust::String(); auto* t = p->getMaster(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String modinst_get_group(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); if (!p) return rust::String(); auto* t = p->getGroup(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String modinst_get_hierarchical_name(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); return p ? rust::String(p->getHierarchicalName()) : rust::String(); }
std::size_t num_modinst_get_mod_i_terms(const OdbDb& h, rust::Str path) { auto* p = gen_modinst(h, path); return p ? p->getModITerms().size() : 0; }
rust::String nth_modinst_get_mod_i_terms(const OdbDb& h, rust::Str path, std::size_t i) { auto* p = gen_modinst(h, path); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModITerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String modnet_get_parent(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_modnet_get_mod_i_terms(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getModITerms().size() : 0; }
rust::String nth_modnet_get_mod_i_terms(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModITerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_modnet_get_mod_b_terms(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getModBTerms().size() : 0; }
rust::String nth_modnet_get_mod_b_terms(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getModBTerms()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_modnet_get_i_terms(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getITerms().size() : 0; }
rust::String nth_modnet_get_i_terms(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getITerms()) { if (k++ == i) return rust::String((e->getInst()->getName() + "/" + e->getMTerm()->getName())); } return rust::String(); }
std::size_t num_modnet_get_b_terms(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getBTerms().size() : 0; }
rust::String nth_modnet_get_b_terms(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getBTerms()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
rust::String modnet_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? rust::String(p->getName()) : rust::String(); }
rust::String modnet_get_const_name(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
rust::String modnet_get_hierarchical_name(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? rust::String(p->getHierarchicalName()) : rust::String(); }
rust::String modnet_find_related_net(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); auto* t = p->findRelatedNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_modnet_get_next_mod_nets_in_fanin(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getNextModNetsInFanin().size() : 0; }
rust::String nth_modnet_get_next_mod_nets_in_fanin(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); auto v = p->getNextModNetsInFanin(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getConstName()); }
std::size_t num_modnet_get_next_mod_nets_in_fanout(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); return p ? p->getNextModNetsInFanout().size() : 0; }
rust::String nth_modnet_get_next_mod_nets_in_fanout(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); auto v = p->getNextModNetsInFanout(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getConstName()); }
rust::String modnet_get_first_parent_mod_net(const OdbDb& h, rust::Str name) { auto* p = gen_modnet(h, name); if (!p) return rust::String(); auto* t = p->getFirstParentModNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String modbterm_get_name(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String modbterm_get_parent(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String modbterm_get_hierarchical_name(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); return p ? rust::String(p->getHierarchicalName()) : rust::String(); }
rust::String modbterm_get_parent_mod_i_term(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); if (!p) return rust::String(); auto* t = p->getParentModITerm(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String modbterm_get_mod_net(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); if (!p) return rust::String(); auto* t = p->getModNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String modbterm_get_sig_type(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); return p ? rust::String(p->getSigType().getString()) : rust::String(); }
rust::String modbterm_get_io_type(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); return p ? rust::String(p->getIoType().getString()) : rust::String(); }
bool modbterm_is_bus_port(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); return p ? p->isBusPort() : false; }
rust::String modbterm_get_mod_inst(const OdbDb& h, rust::Str module, std::size_t idx) { auto* p = gen_modbterm(h, module, idx); if (!p) return rust::String(); auto* t = p->getModInst(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String moditerm_get_name(const OdbDb& h, rust::Str modinst, std::size_t idx) { auto* p = gen_moditerm(h, modinst, idx); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String moditerm_get_parent(const OdbDb& h, rust::Str modinst, std::size_t idx) { auto* p = gen_moditerm(h, modinst, idx); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String moditerm_get_hierarchical_name(const OdbDb& h, rust::Str modinst, std::size_t idx) { auto* p = gen_moditerm(h, modinst, idx); return p ? rust::String(p->getHierarchicalName()) : rust::String(); }
rust::String moditerm_get_mod_net(const OdbDb& h, rust::Str modinst, std::size_t idx) { auto* p = gen_moditerm(h, modinst, idx); if (!p) return rust::String(); auto* t = p->getModNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String moditerm_get_child_mod_b_term(const OdbDb& h, rust::Str modinst, std::size_t idx) { auto* p = gen_moditerm(h, modinst, idx); if (!p) return rust::String(); auto* t = p->getChildModBTerm(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String pwr_domain_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String pwr_domain_get_group(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); auto* t = p->getGroup(); return t ? rust::String(t->getName()) : rust::String(); }
bool pwr_domain_is_top(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); return p ? p->isTop() : false; }
rust::String pwr_domain_get_parent(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); auto* t = p->getParent(); return t ? rust::String(t->getName()) : rust::String(); }
float pwr_domain_get_voltage(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); return p ? p->getVoltage() : 0.0f; }
std::size_t num_pwr_domain_get_power_switches(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); return p ? p->getPowerSwitches().size() : 0; }
rust::String nth_pwr_domain_get_power_switches(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); auto v = p->getPowerSwitches(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
std::size_t num_pwr_domain_get_isolations(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); return p ? p->getIsolations().size() : 0; }
rust::String nth_pwr_domain_get_isolations(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); auto v = p->getIsolations(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
std::size_t num_pwr_domain_get_level_shifters(const OdbDb& h, rust::Str name) { auto* p = gen_pwrdomain(h, name); return p ? p->getLevelShifters().size() : 0; }
rust::String nth_pwr_domain_get_level_shifters(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_pwrdomain(h, name); if (!p) return rust::String(); auto v = p->getLevelShifters(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
rust::String pwr_switch_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_pwrswitch(h, name); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String pwr_switch_get_power_domain(const OdbDb& h, rust::Str name) { auto* p = gen_pwrswitch(h, name); if (!p) return rust::String(); auto* t = p->getPowerDomain(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String pwr_switch_get_lib_cell(const OdbDb& h, rust::Str name) { auto* p = gen_pwrswitch(h, name); if (!p) return rust::String(); auto* t = p->getLibCell(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String isolation_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String isolation_get_applies_to(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? rust::String(p->getAppliesTo()) : rust::String(); }
rust::String isolation_get_clamp_value(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? rust::String(p->getClampValue()) : rust::String(); }
rust::String isolation_get_isolation_signal(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? rust::String(p->getIsolationSignal()) : rust::String(); }
rust::String isolation_get_isolation_sense(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? rust::String(p->getIsolationSense()) : rust::String(); }
rust::String isolation_get_location(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? rust::String(p->getLocation()) : rust::String(); }
rust::String isolation_get_power_domain(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); if (!p) return rust::String(); auto* t = p->getPowerDomain(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_isolation_get_isolation_cells(const OdbDb& h, rust::Str name) { auto* p = gen_isolation(h, name); return p ? p->getIsolationCells().size() : 0; }
rust::String nth_isolation_get_isolation_cells(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_isolation(h, name); if (!p) return rust::String(); auto v = p->getIsolationCells(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getConstName()); }
rust::String level_shifter_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String level_shifter_get_domain(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); if (!p) return rust::String(); auto* t = p->getDomain(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String level_shifter_get_source(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getSource()) : rust::String(); }
rust::String level_shifter_get_sink(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getSink()) : rust::String(); }
bool level_shifter_is_use_functional_equivalence(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? p->isUseFunctionalEquivalence() : false; }
rust::String level_shifter_get_applies_to(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getAppliesTo()) : rust::String(); }
rust::String level_shifter_get_applies_to_boundary(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getAppliesToBoundary()) : rust::String(); }
rust::String level_shifter_get_rule(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getRule()) : rust::String(); }
float level_shifter_get_threshold(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? p->getThreshold() : 0.0f; }
bool level_shifter_is_no_shift(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? p->isNoShift() : false; }
bool level_shifter_is_force_shift(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? p->isForceShift() : false; }
rust::String level_shifter_get_location(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getLocation()) : rust::String(); }
rust::String level_shifter_get_input_supply(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getInputSupply()) : rust::String(); }
rust::String level_shifter_get_output_supply(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getOutputSupply()) : rust::String(); }
rust::String level_shifter_get_internal_supply(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getInternalSupply()) : rust::String(); }
rust::String level_shifter_get_name_prefix(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getNamePrefix()) : rust::String(); }
rust::String level_shifter_get_name_suffix(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getNameSuffix()) : rust::String(); }
rust::String level_shifter_get_cell_name(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getCellName()) : rust::String(); }
rust::String level_shifter_get_cell_input(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getCellInput()) : rust::String(); }
rust::String level_shifter_get_cell_output(const OdbDb& h, rust::Str name) { auto* p = gen_levelshifter(h, name); return p ? rust::String(p->getCellOutput()) : rust::String(); }
rust::String tech_get_name(const OdbDb& h) { auto* p = gen_tech(h); return p ? rust::String(p->getName()) : rust::String(); }
rust::String tech_get_extraction_rules_file(const OdbDb& h) { auto* p = gen_tech(h); return p ? rust::String(p->getExtractionRulesFile()) : rust::String(); }
int32_t tech_get_db_units_per_micron(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getDbUnitsPerMicron() : 0; }
std::size_t num_tech_get_layers(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getLayers().size() : 0; }
rust::String nth_tech_get_layers(const OdbDb& h, std::size_t i) { auto* p = gen_tech(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getLayers()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
rust::String tech_first_frontside_routing_layer(const OdbDb& h) { auto* p = gen_tech(h); if (!p) return rust::String(); auto* t = p->firstFrontsideRoutingLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String tech_first_backside_routing_layer(const OdbDb& h) { auto* p = gen_tech(h); if (!p) return rust::String(); auto* t = p->firstBacksideRoutingLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_tech_get_vias(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getVias().size() : 0; }
rust::String nth_tech_get_vias(const OdbDb& h, std::size_t i) { auto* p = gen_tech(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getVias()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
int32_t tech_get_lef_units(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getLefUnits() : 0; }
double tech_get_lef_version(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getLefVersion() : 0.0; }
rust::String tech_get_lef_version_str(const OdbDb& h) { auto* p = gen_tech(h); return p ? rust::String(p->getLefVersionStr()) : rust::String(); }
bool tech_has_no_wire_ext_at_pin(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->hasNoWireExtAtPin() : false; }
bool tech_has_clearance_measure(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->hasClearanceMeasure() : false; }
bool tech_has_use_min_spacing_obs(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->hasUseMinSpacingObs() : false; }
bool tech_has_use_min_spacing_pin(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->hasUseMinSpacingPin() : false; }
bool tech_has_manufacturing_grid(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->hasManufacturingGrid() : false; }
int32_t tech_get_manufacturing_grid(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getManufacturingGrid() : 0; }
int32_t tech_get_layer_count(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getLayerCount() : 0; }
int32_t tech_get_routing_layer_count(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getRoutingLayerCount() : 0; }
int32_t tech_get_via_count(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getViaCount() : 0; }
std::size_t num_tech_get_non_default_rules(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getNonDefaultRules().size() : 0; }
rust::String nth_tech_get_non_default_rules(const OdbDb& h, std::size_t i) { auto* p = gen_tech(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getNonDefaultRules()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_tech_get_via_rules(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getViaRules().size() : 0; }
rust::String nth_tech_get_via_rules(const OdbDb& h, std::size_t i) { auto* p = gen_tech(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getViaRules()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_tech_get_via_generate_rules(const OdbDb& h) { auto* p = gen_tech(h); return p ? p->getViaGenerateRules().size() : 0; }
rust::String nth_tech_get_via_generate_rules(const OdbDb& h, std::size_t i) { auto* p = gen_tech(h); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getViaGenerateRules()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String lib_get_name(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); return p ? rust::String(p->getName()) : rust::String(); }
rust::String lib_get_const_name(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); if (!p) return rust::String(); const char* v = p->getConstName(); return rust::String(v ? v : ""); }
int32_t lib_get_db_units_per_micron(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); return p ? p->getDbUnitsPerMicron() : 0; }
rust::String lib_get_tech(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); if (!p) return rust::String(); auto* t = p->getTech(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_lib_get_masters(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); return p ? p->getMasters().size() : 0; }
rust::String nth_lib_get_masters(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_lib(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMasters()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
std::size_t num_lib_get_sites(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); return p ? p->getSites().size() : 0; }
rust::String nth_lib_get_sites(const OdbDb& h, rust::Str name, std::size_t i) { auto* p = gen_lib(h, name); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getSites()) { if (k++ == i) return rust::String(e->getConstName()); } return rust::String(); }
int32_t lib_get_lef_units(const OdbDb& h, rust::Str name) { auto* p = gen_lib(h, name); return p ? p->getLefUnits() : 0; }
uint32_t capnode_get_node(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->getNode() : 0; }
uint32_t capnode_get_shape_id(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->getShapeId() : 0; }
bool capnode_is_name(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isName() : false; }
bool capnode_is_i_term(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isITerm() : false; }
bool capnode_is_b_term(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isBTerm() : false; }
bool capnode_is_internal(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isInternal() : false; }
bool capnode_is_branch(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isBranch() : false; }
bool capnode_is_dangling(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isDangling() : false; }
bool capnode_is_foreign(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isForeign() : false; }
bool capnode_is_tree_node(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isTreeNode() : false; }
bool capnode_is_select(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->isSelect() : false; }
uint32_t capnode_incr_children_cnt(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->incrChildrenCnt() : 0; }
uint32_t capnode_get_children_cnt(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->getChildrenCnt() : 0; }
uint32_t capnode_get_sort_index(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->getSortIndex() : 0; }
bool capnode_check_c_c(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); return p ? p->checkCC() : false; }
rust::String capnode_get_net(const OdbDb& h, std::size_t idx) { auto* p = gen_capnode(h, idx); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool rseg_updated_cap(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->updatedCap() : false; }
bool rseg_check_c_c(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->checkCC() : false; }
uint32_t rseg_get_source_node(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->getSourceNode() : 0; }
uint32_t rseg_get_target_node(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->getTargetNode() : 0; }
uint32_t rseg_get_shape_id(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->getShapeId() : 0; }
bool rseg_path_low_to_high(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->pathLowToHigh() : false; }
bool rseg_allocated_cap(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); return p ? p->allocatedCap() : false; }
rust::String rseg_get_net(const OdbDb& h, std::size_t idx) { auto* p = gen_rseg(h, idx); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t rseg_get_coords_x(const OdbDb& h, std::size_t idx) { int v0 = 0; int v1 = 0; auto* p = gen_rseg(h, idx); if (p) p->getCoords(v0, v1); return v0; }
int32_t rseg_get_coords_y(const OdbDb& h, std::size_t idx) { int v0 = 0; int v1 = 0; auto* p = gen_rseg(h, idx); if (p) p->getCoords(v0, v1); return v1; }
uint32_t ccseg_get_source_node_num(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); return p ? p->getSourceNodeNum() : 0; }
uint32_t ccseg_get_target_node_num(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); return p ? p->getTargetNodeNum() : 0; }
rust::String ccseg_get_source_net(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); if (!p) return rust::String(); auto* t = p->getSourceNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String ccseg_get_target_net(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); if (!p) return rust::String(); auto* t = p->getTargetNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
uint32_t ccseg_get_infile_cnt(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); return p ? p->getInfileCnt() : 0; }
bool ccseg_is_marked(const OdbDb& h, std::size_t idx) { auto* p = gen_ccseg(h, idx); return p ? p->isMarked() : false; }
uint32_t sbox_get_via_bottom_layer_mask(const OdbDb& h, rust::Str net, std::size_t swire_idx, std::size_t sbox_idx) { auto* p = gen_sbox(h, net, swire_idx, sbox_idx); return p ? p->getViaBottomLayerMask() : 0; }
uint32_t sbox_get_via_cut_layer_mask(const OdbDb& h, rust::Str net, std::size_t swire_idx, std::size_t sbox_idx) { auto* p = gen_sbox(h, net, swire_idx, sbox_idx); return p ? p->getViaCutLayerMask() : 0; }
uint32_t sbox_get_via_top_layer_mask(const OdbDb& h, rust::Str net, std::size_t swire_idx, std::size_t sbox_idx) { auto* p = gen_sbox(h, net, swire_idx, sbox_idx); return p ? p->getViaTopLayerMask() : 0; }
bool sbox_has_via_layer_masks(const OdbDb& h, rust::Str net, std::size_t swire_idx, std::size_t sbox_idx) { auto* p = gen_sbox(h, net, swire_idx, sbox_idx); return p ? p->hasViaLayerMasks() : false; }
rust::String bpin_get_placement_status(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? rust::String(p->getPlacementStatus().getString()) : rust::String(); }
rust::String bpin_get_b_term(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); if (!p) return rust::String(); auto* t = p->getBTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t bpin_get_b_box_x_min(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().xMin() : 0; }
int32_t bpin_get_b_box_y_min(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().yMin() : 0; }
int32_t bpin_get_b_box_x_max(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().xMax() : 0; }
int32_t bpin_get_b_box_y_max(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().yMax() : 0; }
int32_t bpin_get_b_box_dx(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().dx() : 0; }
int32_t bpin_get_b_box_dy(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getBBox().dy() : 0; }
bool bpin_has_effective_width(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->hasEffectiveWidth() : false; }
int32_t bpin_get_effective_width(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getEffectiveWidth() : 0; }
bool bpin_has_min_spacing(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->hasMinSpacing() : false; }
int32_t bpin_get_min_spacing(const OdbDb& h, rust::Str bterm, std::size_t idx) { auto* p = gen_bpin(h, bterm, idx); return p ? p->getMinSpacing() : 0; }
rust::String mpin_get_m_term(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); if (!p) return rust::String(); auto* t = p->getMTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String mpin_get_master(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); if (!p) return rust::String(); auto* t = p->getMaster(); return t ? rust::String(t->getConstName()) : rust::String(); }
int32_t mpin_get_b_box_x_min(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().xMin() : 0; }
int32_t mpin_get_b_box_y_min(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().yMin() : 0; }
int32_t mpin_get_b_box_x_max(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().xMax() : 0; }
int32_t mpin_get_b_box_y_max(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().yMax() : 0; }
int32_t mpin_get_b_box_dx(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().dx() : 0; }
int32_t mpin_get_b_box_dy(const OdbDb& h, rust::Str master, rust::Str term, std::size_t idx) { auto* p = gen_mpin(h, master, term, idx); return p ? p->getBBox().dy() : 0; }
rust::String techviarule_get_name(const OdbDb& h, std::size_t idx) { auto* p = gen_techviarule(h, idx); return p ? rust::String(p->getName()) : rust::String(); }
uint32_t techviarule_get_via_count(const OdbDb& h, std::size_t idx) { auto* p = gen_techviarule(h, idx); return p ? p->getViaCount() : 0; }
uint32_t techviarule_get_via_layer_rule_count(const OdbDb& h, std::size_t idx) { auto* p = gen_techviarule(h, idx); return p ? p->getViaLayerRuleCount() : 0; }
rust::String techviagenrule_get_name(const OdbDb& h, std::size_t idx) { auto* p = gen_techviagenrule(h, idx); return p ? rust::String(p->getName()) : rust::String(); }
bool techviagenrule_is_default(const OdbDb& h, std::size_t idx) { auto* p = gen_techviagenrule(h, idx); return p ? p->isDefault() : false; }
uint32_t techviagenrule_get_via_layer_rule_count(const OdbDb& h, std::size_t idx) { auto* p = gen_techviagenrule(h, idx); return p ? p->getViaLayerRuleCount() : 0; }
rust::String techvialayerrule_get_layer(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (!p) return rust::String(); auto* t = p->getLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
bool techvialayerrule_has_width(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasWidth() : false; }
bool techvialayerrule_has_enclosure(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasEnclosure() : false; }
bool techvialayerrule_has_overhang(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasOverhang() : false; }
int32_t techvialayerrule_get_overhang(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->getOverhang() : 0; }
bool techvialayerrule_has_metal_overhang(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasMetalOverhang() : false; }
int32_t techvialayerrule_get_metal_overhang(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->getMetalOverhang() : 0; }
bool techvialayerrule_has_rect(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasRect() : false; }
bool techvialayerrule_has_spacing(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasSpacing() : false; }
bool techvialayerrule_has_resistance(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->hasResistance() : false; }
double techvialayerrule_get_resistance(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); return p ? p->getResistance() : 0.0; }
int32_t techvialayerrule_get_width_min_width(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getWidth(v0, v1); return v0; }
int32_t techvialayerrule_get_width_max_width(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getWidth(v0, v1); return v1; }
int32_t techvialayerrule_get_enclosure_overhang1(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getEnclosure(v0, v1); return v0; }
int32_t techvialayerrule_get_enclosure_overhang2(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getEnclosure(v0, v1); return v1; }
int32_t techvialayerrule_get_spacing_x_spacing(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getSpacing(v0, v1); return v0; }
int32_t techvialayerrule_get_spacing_y_spacing(const OdbDb& h, std::size_t gen_idx, std::size_t layer_idx) { int v0 = 0; int v1 = 0; auto* p = gen_techvialayerrule(h, gen_idx, layer_idx); if (p) p->getSpacing(v0, v1); return v1; }
bool layerantenna_is_valid(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->isValid() : false; }
bool layerantenna_has_area_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->hasAreaFactor() : false; }
bool layerantenna_has_side_area_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->hasSideAreaFactor() : false; }
double layerantenna_get_area_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getAreaFactor() : 0.0; }
double layerantenna_get_side_area_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getSideAreaFactor() : 0.0; }
bool layerantenna_is_area_factor_diff_use_only(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->isAreaFactorDiffUseOnly() : false; }
bool layerantenna_is_side_area_factor_diff_use_only(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->isSideAreaFactorDiffUseOnly() : false; }
bool layerantenna_has_antenna_cum_routing_plus_cut(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->hasAntennaCumRoutingPlusCut() : false; }
double layerantenna_get_p_a_r(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getPAR() : 0.0; }
double layerantenna_get_c_a_r(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getCAR() : 0.0; }
double layerantenna_get_p_s_r(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getPSR() : 0.0; }
double layerantenna_get_c_s_r(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getCSR() : 0.0; }
double layerantenna_get_gate_plus_diff_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getGatePlusDiffFactor() : 0.0; }
double layerantenna_get_area_minus_diff_factor(const OdbDb& h, rust::Str layer) { auto* p = gen_layerantenna(h, layer); return p ? p->getAreaMinusDiffFactor() : 0.0; }
int32_t via_params_get_x_cut_size(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXCutSize() : 0; }
int32_t via_params_get_y_cut_size(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYCutSize() : 0; }
int32_t via_params_get_x_cut_spacing(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXCutSpacing() : 0; }
int32_t via_params_get_y_cut_spacing(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYCutSpacing() : 0; }
int32_t via_params_get_x_top_enclosure(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXTopEnclosure() : 0; }
int32_t via_params_get_y_top_enclosure(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYTopEnclosure() : 0; }
int32_t via_params_get_x_bottom_enclosure(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXBottomEnclosure() : 0; }
int32_t via_params_get_y_bottom_enclosure(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYBottomEnclosure() : 0; }
int32_t via_params_get_num_cut_rows(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getNumCutRows() : 0; }
int32_t via_params_get_num_cut_cols(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getNumCutCols() : 0; }
int32_t via_params_get_x_origin(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXOrigin() : 0; }
int32_t via_params_get_y_origin(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYOrigin() : 0; }
int32_t via_params_get_x_top_offset(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXTopOffset() : 0; }
int32_t via_params_get_y_top_offset(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYTopOffset() : 0; }
int32_t via_params_get_x_bottom_offset(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getXBottomOffset() : 0; }
int32_t via_params_get_y_bottom_offset(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); return p ? p->getYBottomOffset() : 0; }
rust::String via_params_get_top_layer(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); if (!p) return rust::String(); auto* t = p->getTopLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_params_get_cut_layer(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); if (!p) return rust::String(); auto* t = p->getCutLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String via_params_get_bottom_layer(const OdbDb& h, rust::Str via) { auto* p = gen_via_params(h, via); if (!p) return rust::String(); auto* t = p->getBottomLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String chip_get_name(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
int32_t chip_get_offset_x(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getOffset().getX() : 0; }
int32_t chip_get_offset_y(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getOffset().getY() : 0; }
int32_t chip_get_width(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getWidth() : 0; }
int32_t chip_get_height(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getHeight() : 0; }
int32_t chip_get_thickness(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getThickness() : 0; }
float chip_get_shrink(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getShrink() : 0.0f; }
int32_t chip_get_seal_ring_east(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getSealRingEast() : 0; }
int32_t chip_get_seal_ring_west(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getSealRingWest() : 0; }
int32_t chip_get_seal_ring_north(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getSealRingNorth() : 0; }
int32_t chip_get_seal_ring_south(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getSealRingSouth() : 0; }
int32_t chip_get_scribe_line_east(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getScribeLineEast() : 0; }
int32_t chip_get_scribe_line_west(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getScribeLineWest() : 0; }
int32_t chip_get_scribe_line_north(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getScribeLineNorth() : 0; }
int32_t chip_get_scribe_line_south(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getScribeLineSouth() : 0; }
bool chip_is_tsv(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->isTsv() : false; }
std::size_t num_chip_get_chip_regions(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getChipRegions().size() : 0; }
rust::String nth_chip_get_chip_regions(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChipRegions()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_chip_get_marker_categories(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getMarkerCategories().size() : 0; }
rust::String nth_chip_get_marker_categories(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getMarkerCategories()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_chip_get_chip_paths(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getChipPaths().size() : 0; }
rust::String nth_chip_get_chip_paths(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChipPaths()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String chip_get_chip_type(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? rust::String(chip_type_str(p->getChipType())) : rust::String(); }
rust::String chip_get_block(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); auto* t = p->getBlock(); return t ? rust::String(t->getConstName()) : rust::String(); }
std::size_t num_chip_get_chip_insts(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getChipInsts().size() : 0; }
rust::String nth_chip_get_chip_insts(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChipInsts()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_chip_get_chip_conns(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getChipConns().size() : 0; }
rust::String nth_chip_get_chip_conns(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChipConns()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
std::size_t num_chip_get_chip_nets(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getChipNets().size() : 0; }
rust::String nth_chip_get_chip_nets(const OdbDb& h, rust::Str chip, std::size_t i) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); std::size_t k = 0; for (auto* e : p->getChipNets()) { if (k++ == i) return rust::String(e->getName()); } return rust::String(); }
rust::String chip_get_tech(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); if (!p) return rust::String(); auto* t = p->getTech(); return t ? rust::String(t->getName()) : rust::String(); }
int32_t chip_get_b_box_x_min(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().xMin() : 0; }
int32_t chip_get_b_box_y_min(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().yMin() : 0; }
int32_t chip_get_b_box_x_max(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().xMax() : 0; }
int32_t chip_get_b_box_y_max(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().yMax() : 0; }
int32_t chip_get_b_box_dx(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().dx() : 0; }
int32_t chip_get_b_box_dy(const OdbDb& h, rust::Str chip) { auto* p = gen_chip(h, chip); return p ? p->getBBox().dy() : 0; }
rust::String chipinst_get_name(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? rust::String(p->getName()) : rust::String(); }
rust::String chipinst_get_orient(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? rust::String(p->getOrient().getString()) : rust::String(); }
rust::String chipinst_get_master_chip(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); if (!p) return rust::String(); auto* t = p->getMasterChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipinst_get_parent_chip(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); if (!p) return rust::String(); auto* t = p->getParentChip(); return t ? rust::String(t->getName()) : rust::String(); }
int32_t chipinst_get_loc_x(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getLoc().x() : 0; }
int32_t chipinst_get_loc_y(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getLoc().y() : 0; }
int32_t chipinst_get_loc_z(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getLoc().z() : 0; }
int32_t chipinst_get_b_box_x_min(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().xMin() : 0; }
int32_t chipinst_get_b_box_y_min(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().yMin() : 0; }
int32_t chipinst_get_b_box_x_max(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().xMax() : 0; }
int32_t chipinst_get_b_box_y_max(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().yMax() : 0; }
int32_t chipinst_get_b_box_dx(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().dx() : 0; }
int32_t chipinst_get_b_box_dy(const OdbDb& h, rust::Str chip, rust::Str inst) { auto* p = gen_chipinst(h, chip, inst); return p ? p->getBBox().dy() : 0; }
rust::String chipregion_get_name(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? rust::String(p->getName()) : rust::String(); }
int32_t chipregion_get_box_x_min(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().xMin() : 0; }
int32_t chipregion_get_box_y_min(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().yMin() : 0; }
int32_t chipregion_get_box_x_max(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().xMax() : 0; }
int32_t chipregion_get_box_y_max(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().yMax() : 0; }
int32_t chipregion_get_box_dx(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().dx() : 0; }
int32_t chipregion_get_box_dy(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? p->getBox().dy() : 0; }
rust::String chipregion_get_chip(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); if (!p) return rust::String(); auto* t = p->getChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipregion_get_side(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); return p ? rust::String(chip_region_side_str(p->getSide())) : rust::String(); }
rust::String chipregion_get_layer(const OdbDb& h, rust::Str chip, rust::Str region) { auto* p = gen_chipregion(h, chip, region); if (!p) return rust::String(); auto* t = p->getLayer(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String chipregioninst_get_chip_inst(const OdbDb& h, rust::Str chip, rust::Str inst, rust::Str region) { auto* p = gen_chipregioninst(h, chip, inst, region); if (!p) return rust::String(); auto* t = p->getChipInst(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipregioninst_get_chip_region(const OdbDb& h, rust::Str chip, rust::Str inst, rust::Str region) { auto* p = gen_chipregioninst(h, chip, inst, region); if (!p) return rust::String(); auto* t = p->getChipRegion(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipbump_get_chip(const OdbDb& h, rust::Str chip, rust::Str region, std::size_t idx) { auto* p = gen_chipbump(h, chip, region, idx); if (!p) return rust::String(); auto* t = p->getChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipbump_get_chip_region(const OdbDb& h, rust::Str chip, rust::Str region, std::size_t idx) { auto* p = gen_chipbump(h, chip, region, idx); if (!p) return rust::String(); auto* t = p->getChipRegion(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String chipbump_get_inst(const OdbDb& h, rust::Str chip, rust::Str region, std::size_t idx) { auto* p = gen_chipbump(h, chip, region, idx); if (!p) return rust::String(); auto* t = p->getInst(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String chipbump_get_net(const OdbDb& h, rust::Str chip, rust::Str region, std::size_t idx) { auto* p = gen_chipbump(h, chip, region, idx); if (!p) return rust::String(); auto* t = p->getNet(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String chipbump_get_b_term(const OdbDb& h, rust::Str chip, rust::Str region, std::size_t idx) { auto* p = gen_chipbump(h, chip, region, idx); if (!p) return rust::String(); auto* t = p->getBTerm(); return t ? rust::String(t->getConstName()) : rust::String(); }
rust::String chipconn_get_name(const OdbDb& h, rust::Str chip, rust::Str conn) { auto* p = gen_chipconn(h, chip, conn); return p ? rust::String(p->getName()) : rust::String(); }
int32_t chipconn_get_thickness(const OdbDb& h, rust::Str chip, rust::Str conn) { auto* p = gen_chipconn(h, chip, conn); return p ? p->getThickness() : 0; }
rust::String chipconn_get_parent_chip(const OdbDb& h, rust::Str chip, rust::Str conn) { auto* p = gen_chipconn(h, chip, conn); if (!p) return rust::String(); auto* t = p->getParentChip(); return t ? rust::String(t->getName()) : rust::String(); }
std::size_t num_chipconn_get_top_region_path(const OdbDb& h, rust::Str chip, rust::Str conn) { auto* p = gen_chipconn(h, chip, conn); return p ? p->getTopRegionPath().size() : 0; }
rust::String nth_chipconn_get_top_region_path(const OdbDb& h, rust::Str chip, rust::Str conn, std::size_t i) { auto* p = gen_chipconn(h, chip, conn); if (!p) return rust::String(); auto v = p->getTopRegionPath(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
std::size_t num_chipconn_get_bottom_region_path(const OdbDb& h, rust::Str chip, rust::Str conn) { auto* p = gen_chipconn(h, chip, conn); return p ? p->getBottomRegionPath().size() : 0; }
rust::String nth_chipconn_get_bottom_region_path(const OdbDb& h, rust::Str chip, rust::Str conn, std::size_t i) { auto* p = gen_chipconn(h, chip, conn); if (!p) return rust::String(); auto v = p->getBottomRegionPath(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
rust::String chipnet_get_name(const OdbDb& h, rust::Str chip, rust::Str net) { auto* p = gen_chipnet(h, chip, net); return p ? rust::String(p->getName()) : rust::String(); }
rust::String chipnet_get_chip(const OdbDb& h, rust::Str chip, rust::Str net) { auto* p = gen_chipnet(h, chip, net); if (!p) return rust::String(); auto* t = p->getChip(); return t ? rust::String(t->getName()) : rust::String(); }
uint32_t chipnet_get_num_bump_insts(const OdbDb& h, rust::Str chip, rust::Str net) { auto* p = gen_chipnet(h, chip, net); return p ? p->getNumBumpInsts() : 0; }
rust::String chippath_get_name(const OdbDb& h, rust::Str chip, rust::Str path) { auto* p = gen_chippath(h, chip, path); if (!p) return rust::String(); const char* v = p->getName(); return rust::String(v ? v : ""); }
rust::String chippath_get_chip(const OdbDb& h, rust::Str chip, rust::Str path) { auto* p = gen_chippath(h, chip, path); if (!p) return rust::String(); auto* t = p->getChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String unfoldedchip_get_name(const OdbDb& h, rust::Str path) { auto* p = gen_unfoldedchip(h, path); return p ? rust::String(p->getName()) : rust::String(); }
std::size_t num_unfoldedchip_get_chip_inst_path(const OdbDb& h, rust::Str path) { auto* p = gen_unfoldedchip(h, path); return p ? p->getChipInstPath().size() : 0; }
rust::String nth_unfoldedchip_get_chip_inst_path(const OdbDb& h, rust::Str path, std::size_t i) { auto* p = gen_unfoldedchip(h, path); if (!p) return rust::String(); auto v = p->getChipInstPath(); if (i >= v.size()) return rust::String(); auto* e = v[i]; return rust::String(e->getName()); }
rust::String unfoldedregion_get_parent_chip(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); if (!p) return rust::String(); auto* t = p->getParentChip(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String unfoldedregion_get_effective_side(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? rust::String(unfolded_side_str(p->getEffectiveSide())) : rust::String(); }
bool unfoldedregion_is_top(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? p->isTop() : false; }
bool unfoldedregion_is_bottom(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? p->isBottom() : false; }
bool unfoldedregion_is_internal(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? p->isInternal() : false; }
bool unfoldedregion_is_internal_ext(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? p->isInternalExt() : false; }
int32_t unfoldedregion_get_surface_z(const OdbDb& h, rust::Str path, std::size_t idx) { auto* p = gen_unfoldedregion(h, path, idx); return p ? p->getSurfaceZ() : 0; }
int32_t unfoldedbump_get_global_position_x(const OdbDb& h, rust::Str path, std::size_t region_idx, std::size_t idx) { auto* p = gen_unfoldedbump(h, path, region_idx, idx); return p ? p->getGlobalPosition().x() : 0; }
int32_t unfoldedbump_get_global_position_y(const OdbDb& h, rust::Str path, std::size_t region_idx, std::size_t idx) { auto* p = gen_unfoldedbump(h, path, region_idx, idx); return p ? p->getGlobalPosition().y() : 0; }
int32_t unfoldedbump_get_global_position_z(const OdbDb& h, rust::Str path, std::size_t region_idx, std::size_t idx) { auto* p = gen_unfoldedbump(h, path, region_idx, idx); return p ? p->getGlobalPosition().z() : 0; }
rust::String unfoldedconn_get_chip_conn(const OdbDb& h, std::size_t idx) { auto* p = gen_unfoldedconn(h, idx); if (!p) return rust::String(); auto* t = p->getChipConn(); return t ? rust::String(t->getName()) : rust::String(); }
rust::String unfoldednet_get_chip_net(const OdbDb& h, std::size_t idx) { auto* p = gen_unfoldednet(h, idx); if (!p) return rust::String(); auto* t = p->getChipNet(); return t ? rust::String(t->getName()) : rust::String(); }
